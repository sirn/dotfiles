---
image: archlinux
sources:
  - https://git.sr.ht/~sirn/dotfiles
tasks:
  - prepare: |
      sudo pacman -Sy
      sudo pacman -S --noconfirm base-devel
  - setup: |
      cd dotfiles
      env AUR_CHROOT=n ./bootstrap.sh
