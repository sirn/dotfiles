#!/bin/sh

case "$(ibus engine)" in
    xkb:us:* )
        ibus engine libthai || true
        ;;
    libthai )
        ibus engine mozc-jp || true
        ;;
    mozc-jp )
        ibus engine xkb:us::eng || true
        ;;
esac
