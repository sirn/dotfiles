#!/bin/sh
#
# Runs Magit in standalone mode
#

EMACS_CONFIG_DIR="$(
    cd "$(dirname "$0")/../etc/emacs" || exit 1
    pwd -P
)"

if [ "$(git rev-parse --is-inside-work-tree)" = "true" ]; then
    exec emacs -Q -nw -l "$EMACS_CONFIG_DIR/magit.el"
fi
