#!/usr/bin/env bash

if [[ "$(uname)" = "Darwin" ]]; then
    if hash reattach-to-user-namespace 2>/dev/null; then
        reattach-to-user-namespace $@
    else
        exec "$@"
    fi
else
    exec "$@"
fi
