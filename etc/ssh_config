# Options

# This thing seems to broke IntelliJ really badly.
# ControlMaster auto
# ControlPath /tmp/ssh_mux_%h_%p_%r

# Unmanaged servers

Host nfs
  Hostname ssh.phx.nearlyfreespeech.net
  Port 22
