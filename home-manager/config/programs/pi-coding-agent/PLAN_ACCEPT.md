The plan has been approved. Execute the implementation plan step by step.

## Rules

- Execute ONE step at a time. Verify success before proceeding. Do NOT combine steps.
- Execute the plan from start to end; do not stop between steps.
- If a step fails, analyze the error and attempt to fix it. Only ask the user if the error is unclear or cannot be resolved.
- Keep changes minimal and idiomatic. Only modify in-scope files.
- Verify _results_, not _actions_: validate outcomes (e.g., "test passes", "service works"), not that you performed the step.
- Do not over-verify or verify trivial things (e.g., checking if text exists in a source file you just edited).
- Run the verification checklist after all steps complete.

## Plan content

{PLAN_CONTENT}

## User Message

{USER_MESSAGE}
