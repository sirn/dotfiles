---
- name: update homebrew
  homebrew: update_homebrew=yes

- name: install homebrew packages
  homebrew: name={{item}} state=latest
  with_items: packages

- name: install ffmpeg with fdk-aac
  homebrew:
    name: ffmpeg
    install_options: with-fdk-aac
    state: latest
